# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Database (local only — Railway uses /data volume)
server/database.sqlite

# Uploaded images (local only — Railway uses /data/uploads volume)
server/uploads/

# Environment secrets
.env
server/.env
client/.env

# Build output (generated at deploy time)
client/dist/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
